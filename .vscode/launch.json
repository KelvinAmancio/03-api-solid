{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "node",
            "request": "attach",
            "name": "Docker Nodemon TypeScript Attach",
            "remoteRoot": "/app",
            "restart": true,
            "outFiles": ["${workspaceFolder}/build/**/*.js"]
        }
    ]
}